# Legacy Assets

This directory contains legacy Visual Studio solution/project files and demo-style tests retained for developer convenience.

- Canonical OSS build/test path is CMake (`CMakeLists.txt`, presets, and `tests/`).
- Legacy VS assets are secondary and are not used by CI.
- Legacy solution path: `legacy/vs/msl_tests.sln`.
